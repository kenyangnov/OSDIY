本次任务：进行内存检查

主要思路：往内存中随便写一个值，然后马上读取，来检查读取的值和写入的值是否相等。

结果：如果内存连接正常，则写入的值能够记录在内存里。如果没连接上，则读出的值肯定是乱七八糟的。

注意：首先应将缓存设置为OFF。因为如果CPU里加上了缓存，写入和读出的不是内存，而是缓存。结果所有内存都“正常”，检查处理不能完成。

我们对0x00400000~0xbfffffff范围内的内存进行检查。0c00400000号以前的内存已经被使用了，没有内存程序根本运行不到这里，所以我们没做内存检查。

理论上应该显示32M，因为模拟器的设定是这么大。
