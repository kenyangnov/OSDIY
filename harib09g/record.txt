我们为了加快处理速度，将TIMECTL结构进行了进一步改善，我们新增了一个计时器表，存放按某种顺序排好的定时器地址，这样就不用每次都查找一遍最“近”的计时器了。效仿图层结构中的top属性，我们又增加了一个using属性，用于记录现在的定时器有几个处于活动中。
这样在超时的情况下，直接按照计时器表移动到下一个计时器就行了，不必每次查找下一个时刻了，或者查找有没有别的定时器超时了。

另外，需要更新的几个函数有init_pit函数，time_alloc函数，稍复杂一点的是timer_settime函数，因为我们必须将timer注册到timers中正确的位置上。